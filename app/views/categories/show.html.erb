<%= link_to "All Categories", categories_path %>
<h1>Category Details</h1>

<p><%= @category.name %></p>
<%= link_to "Edit", edit_category_path(@category) %>

<%= button_to "Delete", @category, method: :delete , data: {confirm: 'Are you sure?'} %>